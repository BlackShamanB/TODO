// import { Observable, fromEvent } from 'rxjs';

// document.addEventListener('DOMContentLoaded', () => {
//         const dragAndDrop$: Observable<Event> = fromEvent<Event>(
//           document.querySelector('todo') as HTMLButtonElement,
//           'click'
//         );
      
//         dragAndDrop$.pipe().subscribe((event) => {
//           console.log(event);
//         });
// })

